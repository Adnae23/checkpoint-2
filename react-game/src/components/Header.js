import React from 'react';

const Header = (props) => {
    return (
        <div>
            <h1>{props.name}</h1>
            <h4>Bienvenue dans la Team !!!! a revoir pour etre drole. Je sais, je sais... encore un message de bienvenue,pff</h4>
        </div>
    );
};

export default Header;